procedure main is
	i:=10;
	do print_three( 3, 5, i );
	do print(i);
end main;

procedure print_three(x: integer; y: integer; z: integer) is
	do print(x);
	do print(y);
	do print(z);
	z := 100; -- observe the in mode
end print_three;
