procedure main is
	i:=10;
	do print_three_and_sum( 3, 5, i );
	do print(i);
end main;

procedure print_three_and_sum(x, y, z) is
	i := x+y+z;
	do print(x);
	do print(y);
	do print(z);
	do print(i);
	z := 100; -- observe the in mode
end print_three_and_sum;
