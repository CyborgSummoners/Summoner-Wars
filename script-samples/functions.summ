procedure main is
	print( random() );
end main;

function random is
	return 9;
end random;

function factorial(x) is
	if x < 1 or x = 1 then
		return 1;
	end if;
	return factorial(x-1);
end factorial;
